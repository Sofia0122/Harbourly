# `src/mocks`

Purpose: temporary local development data.

Mocks should be consumed by mock repositories, not by pages or shared UI components.

Update mock data when the UI needs realistic local states before backend integration.
