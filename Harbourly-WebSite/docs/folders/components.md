# `src/components`

Purpose: shared reusable components.

Subfolders:

- `layout`: public/admin shells, header, sidebar, navigation and route guards.
- `ui`: generic primitives such as Button, Card, Modal, Input, Select and Table.

Business-specific components belong in `src/features/<feature>/components`, not in shared UI primitives.
