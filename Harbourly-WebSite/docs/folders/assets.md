# `src/assets`

Purpose: assets imported by frontend source code.

Use this folder for images and files bundled by Vite imports. Use `public/` for files that need stable public paths.

Avoid unused starter assets, duplicate images and mock data in this folder.
