# `src/types`

Purpose: cross-feature TypeScript types.

Only place types here when they are genuinely shared across multiple features. Feature-specific models belong in the related feature `types/` folder.
