# `src/pages`

Purpose: route-level components.

Pages compose layouts and feature components. They should stay thin and should not call repositories, `fetch`, mocks or business logic directly.

When adding a page, update route constants, router configuration and routing docs.
